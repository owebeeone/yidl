"""
Hand-crafted “generated” lifecycle module (target shape for the YIDL compiler).

Augment this file as each IMPL_PROGRESS slice lands. Parity tests import from here
when ``LC_PARITY_IMPL=handcrafted``.
"""

# Intentionally minimal until infra_default_tx / managed slices are implemented.
